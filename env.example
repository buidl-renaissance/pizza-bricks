# Database Configuration
# For local development, set USE_LOCAL=true to use SQLite file
USE_LOCAL=true

# For production with Turso
TURSO_DATABASE_URL=libsql://your-database.turso.io
TURSO_AUTH_TOKEN=your-auth-token

# Pizza Bricks Landing
NEXT_PUBLIC_DISCORD_INVITE=https://discord.gg/your-invite

# Supabase (waitlist / landing signups)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# --- Outreach System ---

# Google Places API (business discovery)
GOOGLE_PLACES_API_KEY=your-places-api-key

# Serper.dev (search for email, social profiles, websites)
SERPER_API_KEY=your-serper-api-key

# Anthropic API (email drafting)
ANTHROPIC_API_KEY=your-anthropic-api-key

# Gemini API (kept for reference, replaced by Anthropic)
GEMINI_API_KEY=your-gemini-api-key

# Gmail API (OAuth 2.0 — credentials saved to .gmail-credentials.json after first login)
GMAIL_CLIENT_ID=your-gmail-client-id
GMAIL_CLIENT_SECRET=your-gmail-client-secret

# Demo mode: redirect ALL outgoing emails to this address (remove/leave blank for production)
DEMO_EMAIL_RECIPIENT=

# Default search location for vendor discovery
OUTREACH_DEFAULT_LOCATION=Denver, CO

# --- Base Autonomous Agent ---

# OnchainKit (wallet UI components)
NEXT_PUBLIC_ONCHAINKIT_API_KEY=your-onchainkit-api-key

# Coinbase Developer Platform (server-side wallet operations)
CDP_API_KEY_ID=your-cdp-api-key-id
CDP_API_KEY_SECRET=your-cdp-api-key-secret

# ERC-8021 Builder Code (register at base.dev)
BUILDER_CODE=your-builder-code
# Exposed to browser for client-side calldata attribution
NEXT_PUBLIC_BUILDER_CODE=your-builder-code

# Agent wallet that receives onboarding fees on Base mainnet
# Derived from DEPLOYER_PRIVATE_KEY — run: node -e "const {ethers}=require('ethers');console.log(new ethers.Wallet('0x<KEY>').address)"
NEXT_PUBLIC_AGENT_WALLET_ADDRESS=0xYourAgentWalletAddress

# Secret to protect the autonomous cron endpoint
AGENT_CRON_SECRET=your-random-secret

# --- Menu Item Buy Links (bitebitebot.vercel.app) ---
# Base URL for menu item buy links (default: https://bitebitebot.vercel.app)
# BITE_BITE_BASE_URL=https://bitebitebot.vercel.app
# Recipient wallet address for payments (can be overridden per prospect in metadata)
# BITE_BITE_RECIPIENT_ADDRESS=0x...

# --- Token Deployment (ADI + Hedera) ---

# Private key of the deployer wallet (hex, no 0x prefix)
DEPLOYER_PRIVATE_KEY=your-deployer-wallet-private-key

# ADI Network RPC — omit this var to use testnet (default), set it for mainnet
# Testnet (default): https://rpc.ab.testnet.adifoundation.ai/  (chain ID 99999)
# Mainnet:           https://rpc.adifoundation.ai
# ADI_RPC_URL=https://rpc.adifoundation.ai
